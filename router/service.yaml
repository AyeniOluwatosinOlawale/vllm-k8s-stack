apiVersion: v1
kind: Service
metadata:
  name: vllm-router
  namespace: llm-serving
  labels:
    app: vllm-router
spec:
  type: ClusterIP
  selector:
    app: vllm-router
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
